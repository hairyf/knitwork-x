---
title: decorator
description: Decorator generation (genDecorator)
icon: i-lucide-square-function
---

This module provides APIs for generating decorator syntax `@Decorator` or `@Decorator(args)`.

<example module="decorator"></example>

<!-- automd:jsdocs src="../../src/decorator.ts" -->

## Typescript

### `genDecorator(name, args?, indent)`

Generate decorator: `@decorator` or `@decorator(args)`.

**Example:**

```js
genDecorator("Component");
// ~> `@Component`

genDecorator("Injectable", "()");
// ~> `@Injectable()`

genDecorator("Route", '("/api")');
// ~> `@Route("/api")`

genDecorator("Validate", "(min: 0, max: 100)");
// ~> `@Validate(min: 0, max: 100)`
```

<!-- /automd -->
