---
title: statement
description: Statement generation (genReturn, genThrow)
icon: i-lucide-square-function
---

This module provides APIs for generating `return` and `throw` statements.

<example module="statement"></example>

<!-- automd:jsdocs src="../../src/statement.ts" -->

## Typescript

### `genReturn(expr?, indent)`

Generate `return expr;` or `return;`.

**Example:**

```js
genReturn("x");
// ~> `return x;`

genReturn();
// ~> `return;`

genReturn("a + b");
// ~> `return a + b;`
```

### `genThrow(expr, indent)`

Generate `throw expr;`.

**Example:**

```js
genThrow("new Error('failed')");
// ~> `throw new Error('failed');`

genThrow("e");
// ~> `throw e;`
```

<!-- /automd -->
